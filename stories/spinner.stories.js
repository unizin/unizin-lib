/* @flow */
import React from 'react';
import Spinner from '../src/components/spinner';
import { storiesOf } from '@storybook/react';

storiesOf('Spinner', module).add('Defaults', () => <Spinner />);
